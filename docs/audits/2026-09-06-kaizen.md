# Business-plan engine Kaizen — 2026-09-06

Scope: the canonical `C:/wamp64/www/business-plan-skills` checkout. Local,
uncommitted improvements; no client plan, posting, publication or professional
approval was performed. Reporting basis for this tooling audit: not applicable.

## Baseline and experiment

The native inventory found 126 active skills, with one broken Digital Research
currentness-gate link. The mandatory root route also pointed to the obsolete
directory. Routing passed all 40 top-three fixtures; the test suite passed 26
tests. Code inspection found the release gate dereferenced a non-object JSON
root and hashed unvalidated nested IDs/states. Missing `required` flags could
also silently avoid a required-gate decision.

Hypothesis: canonical references plus typed release-envelope validation remove
the observed route failure and make malformed inputs explicit findings without
changing legitimate blocked/release semantics. Main changes are limited to
the root guide, Kaizen link, release validator and regression tests.

## Acceptance

| Executed check | Result |
|---|---|
| Native validator against zero-debt baseline | 126/126 compliant; no failures |
| Routing | 40/40 top-three fixtures |
| Unit suite | 30 tests passed, including malformed-root/nested-field, omitted-required-flag and workbook CLI-alias cases |
| Changed Kaizen quick validator | Passed |
| Canonical cross-engine compliance scan | 125/126; one lexical degraded-mode finding |
| Diff whitespace | Passed |

The canonical scanner's residual finding is a false negative for
`business-plan-orchestrator`: its explicit Degraded Mode section says to
continue with verified inputs, mark unavailable checks not-assessed, and block
mandatory release. The scanner recognises only a few phrases and misses this
wording. The local validator accepts the actual contract. No extra boilerplate
was added to manipulate that count.

The independent [readiness review](2026-09-06-kaizen-readiness.md) records
exemplar, source, sector and workbook checks and their limitations. The review
is complete: seven workbook structures/formula maps pass, but formula caches
are not recalculated and four release bundles remain blocked. The documented
--output CLI alias now works alongside --json-out; production workbooks were
not changed.

## Assessment and next cycle

Doctrine and routing are structurally assessed through the read router,
orchestrator and native gates. Skill depth is sampled, not a complete semantic
census. Applied examples and all output families are bounded by the readiness
record. Current market, legal, accounting and provider claims are NOT ASSESSED;
this repair admits no new external claim. Inclusion, rendered documents/decks,
client model fitness, stakeholder acceptance and production outcomes are NOT
ASSESSED. Overall numerical readiness is withheld rather than inferred from
headings or test counts. The policy ceiling remains 65/100; 95/100 is a target.

P0: keep malformed inputs failing and required-gate applicability explicit.
P1: execute a connected plan/model/render/reviewer handoff using sanitised
fixtures; retain all release blockers. P2: improve the shared scanner's phrase
detection with semantic counterexamples rather than changing sound skill text.
No new skill is justified by these defects.

Proposed owner: engine maintainer and relevant release reviewer. Re-audit:
2026-09-13 or after schema, route or release-policy changes. Stop if a malformed
bundle is accepted or an unavailable mandatory check is treated as passed.
Recovery: reverse only the scoped validator/test/link patch and rerun the same
positive and negative fixtures. Preserve unrelated work and evidence history.
