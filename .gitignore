# Proposal workspaces are confidential — never committed to the shared repo
/proposals/

# Stale local mirror of the finance engine (canonical: C:\wamp64\www\chwezi-accounting-doctrine; never commit)
/doctrine/

# Local Python execution caches
__pycache__/
*.py[cod]
